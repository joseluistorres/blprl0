# SQLite version 3.x
#   gem install sqlite3
development:
  adapter: mysql
  encoding: utf8
  reconnect: false
  database: blackpearl_dev
  pool: 5
  username: root
  password: rebecca
  socket: /tmp/mysql.sock

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
    adapter: mysql
    encoding: utf8
    reconnect: false
    database: blackpearl_test
    pool: 5
    username: root
    password: rebecca
    socket: /tmp/mysql.sock

production:
    adapter: mysql
    encoding: utf8
    reconnect: false
    database: blackpearl_prod
    pool: 5
    username: root
    password: rebecca
    socket: /var/lib/mysql/mysql.sock
